FitBrand v21 load-safe build

This package keeps files at the ZIP root so index.html is easy to open/upload.
The newest risky v20 changes were removed. This version is based on the stable v19 build, with only safe final polish added.
External hero image dependencies were removed so the website can load faster even in local preview or slow hosting.
